<?php

/**
 * Get Greatest-Common-Divisor
 * This function follows the Euclid algorithm specification found at https://en.wikipedia.org/wiki/Euclidean_algorithm.
 */
function get_GCD ($n1, $n2): int {
    $gcd = 0;
    $c_divisor = $n1 < $n2 ? $n1 : $n2;
    $
    $remainder = 1;

    while ($remainder) {

    }

    return $gcd;
}

echo get_GCD(1071, 462);
